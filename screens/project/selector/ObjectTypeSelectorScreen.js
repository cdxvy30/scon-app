import React from 'react';
import {Text} from 'react-native';

const ObjectTypeSelectorScreen = ({navigation, route}) => {
  return (
    <React.Fragment>
      <Text>This is object type selector</Text>
    </React.Fragment>
  );
};

export default ObjectTypeSelectorScreen;
